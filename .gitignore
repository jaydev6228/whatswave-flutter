# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
coverage/
pubspec.lock

# IDE
.idea/
.vscode/
*.iml

# macOS
.DS_Store
devtools_options.yaml

# Environment / secrets
.env
.env.*
!.env.example
config/dev.json
ios/Flutter/Secrets.xcconfig
*.pem
*.p12
*.cer
*.mobileprovision
*.keystore
*.jks
key.properties
android/key.properties
serviceAccountKey*.json
*-firebase-adminsdk-*.json
*.credentials.json

# Firebase (client config kept local-only, since this repo is public;
# regenerate with `flutterfire configure` per docs/handoff/03_firebase_dev_setup.md)
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
macos/Runner/GoogleService-Info.plist
lib/firebase_options.dart
firebase.json
.firebaserc
.firebase/
firebase-debug.log
firebase-debug.*.log

# LiveKit token server (server/livekit-token) -- Node/Vercel project
server/livekit-token/node_modules/
server/livekit-token/.vercel/
server/livekit-token/.env
server/livekit-token/.env.*
!server/livekit-token/.env.example

# Internal project handoff/setup notes -- not meant for a public repo.
# Kept locally, no longer tracked (see git rm --cached history for when
# these stopped being committed).
CLAUDE.md
PROJECT_BRIEF_FOR_NEW_DEVELOPER.md
TRANSFER_TO_NEW_PC.md
docs/handoff/
